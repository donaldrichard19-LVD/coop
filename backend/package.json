{
  "name": "coop-backend",
  "version": "0.0.0",
  "description": "Coop backend — RCS messaging channel (first of three planned: RCS, WhatsApp, Apple Messages for Business)",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node --test rcs/*.test.js",
    "provision:rcs": "node scripts/provisionTemplates.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "resend": "^6.12.3",
    "twilio": "^5.3.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
